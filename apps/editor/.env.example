# CanvasFlow editor — Environment variables template

NODE_ENV=development

# api-gateway base URL (used in PR #17 when documents load from the API)
VITE_API_URL=http://localhost:3001

# web app base URL — used to silently refresh the editor auth token
VITE_WEB_URL=http://localhost:3000
